name: Coolify Relay Node

on:
  workflow_dispatch:
    inputs:
      cycle_count:
        description: 'Relay cycle counter'
        required: false
        default: '0'
        type: string
      retry_depth:
        description: 'Pre-warming circuit breaker retry depth'
        required: false
        default: '0'
        type: string
      test_handover:
        description: 'Test mode: hand over after X minutes (0 for normal 330m)'
        required: false
        default: '0'
        type: string

jobs:
  coolify-relay:
    name: Run Persistent Coolify Relay Node
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout Orchestration Repository
        uses: actions/checkout@v4

      # =====================================================================
      # PHASE 1: PRE-WARMING & SYSTEM INITIALIZATION
      # =====================================================================
      - name: Bootstrap System, Dependencies & Pre-Warm State
        id: prewarm
        env:
          CLONE_PAT: ${{ secrets.CLONE_PAT }}
          RCLONE_CONFIG_BASE64: ${{ secrets.RCLONE_CONFIG_BASE64 }}
          STORAGE_TARGET: ${{ secrets.STORAGE_TARGET }}
          CYCLE_COUNT: ${{ github.event.inputs.cycle_count }}
        run: |
          set -euo pipefail
          echo "=== PHASE 1: Pre-Warming System, Local Images & Baseline State ==="
          CYCLE="${CYCLE_COUNT:-0}"

          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            curl jq rclone pigz netcat-openbsd sqlite3 dnsutils ca-certificates openssh-server

          # Install cloudflared (direct official release deb)
          echo "Installing Cloudflare Tunnel client (cloudflared)..."
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o /tmp/cloudflared.deb
          sudo dpkg -i /tmp/cloudflared.deb
          cloudflared --version

          # Restore Rclone configuration
          mkdir -p ~/.config/rclone
          sudo mkdir -p /root/.config/rclone
          if [ -n "$RCLONE_CONFIG_BASE64" ]; then
            echo "$RCLONE_CONFIG_BASE64" | base64 -d > ~/.config/rclone/rclone.conf
            sudo cp -f ~/.config/rclone/rclone.conf /root/.config/rclone/rclone.conf
            chmod 600 ~/.config/rclone/rclone.conf
            sudo chmod 600 /root/.config/rclone/rclone.conf
            echo "Rclone credentials restored for Google Drive."
          else
            echo "CRITICAL: RCLONE_CONFIG_BASE64 secret is missing!"
            exit 1
          fi

          TARGET="${STORAGE_TARGET:-gdrive:coolify-relay-state/coolify-state}"
          sudo mkdir -p /data/coolify /var/lib/docker/volumes /etc/cloudflared /tmp/prewarm_cache
          sudo chmod 777 /tmp/prewarm_cache

          # Validate Google Drive Connectivity
          echo "Probing Google Drive connectivity at ${TARGET}..."
          if ! rclone lsf --fast-list "$TARGET" --max-depth 1 >/tmp/prewarm_ls.txt 2>&1; then
            echo "CRITICAL: Google Drive connection failed during Phase 1 pre-warm!"
            cat /tmp/prewarm_ls.txt
            exit 1
          fi

          # Fail-Closed Guard: For any cycle > 0, state recovery is mandatory
          if [ "$CYCLE" != "0" ]; then
            echo "Cycle ${CYCLE} detected. Enforcing mandatory state retrieval..."
            if ! grep -q "coolify_bundle.tar.gz" /tmp/prewarm_ls.txt; then
              echo "CRITICAL: Cycle is ${CYCLE} but 'coolify_bundle.tar.gz' is missing from Google Drive!"
              echo "Halting pre-warm to prevent blank state overwrite."
              exit 1
            fi

            echo "Fetching baseline bundles from Google Drive..."
            rclone copy "$TARGET" /tmp/prewarm_cache \
              --include "coolify_bundle.tar.gz" \
              --include "volumes_bundle.tar.gz" \
              --include "coolify_pg_latest.sql.gz" \
              --drive-chunk-size 128M \
              --fast-list

            if [ -f "/tmp/prewarm_cache/coolify_bundle.tar.gz" ]; then
              echo "Decompressing core state with 4-core pigz..."
              pigz -dc -p 4 /tmp/prewarm_cache/coolify_bundle.tar.gz | sudo tar --numeric-owner -xpf - -C /data/coolify || true
            fi
            if [ -f "/tmp/prewarm_cache/volumes_bundle.tar.gz" ]; then
              echo "Decompressing application volumes with 4-core pigz..."
              pigz -dc -p 4 /tmp/prewarm_cache/volumes_bundle.tar.gz | sudo tar --numeric-owner -xpf - -C /var/lib/docker/volumes || true
            fi
            if [ -f "/tmp/prewarm_cache/coolify_pg_latest.sql.gz" ]; then
              sudo mkdir -p /data/coolify/backups
              sudo cp -f /tmp/prewarm_cache/coolify_pg_latest.sql.gz /data/coolify/backups/coolify_pg_latest.sql.gz
            fi
            sudo rm -rf /tmp/prewarm_cache
          else
            # Cycle 0: Check if existing baseline exists on Google Drive
            if grep -q "coolify_bundle.tar.gz" /tmp/prewarm_ls.txt; then
              echo "Cycle 0 with existing Google Drive baseline detected. Hydrating..."
              rclone copy "$TARGET" /tmp/prewarm_cache \
                --include "coolify_bundle.tar.gz" \
                --include "volumes_bundle.tar.gz" \
                --include "coolify_pg_latest.sql.gz" \
                --drive-chunk-size 128M \
                --fast-list

              [ -f "/tmp/prewarm_cache/coolify_bundle.tar.gz" ] && pigz -dc -p 4 /tmp/prewarm_cache/coolify_bundle.tar.gz | sudo tar --numeric-owner -xpf - -C /data/coolify || true
              [ -f "/tmp/prewarm_cache/volumes_bundle.tar.gz" ] && pigz -dc -p 4 /tmp/prewarm_cache/volumes_bundle.tar.gz | sudo tar --numeric-owner -xpf - -C /var/lib/docker/volumes || true
              if [ -f "/tmp/prewarm_cache/coolify_pg_latest.sql.gz" ]; then
                sudo mkdir -p /data/coolify/backups
                sudo cp -f /tmp/prewarm_cache/coolify_pg_latest.sql.gz /data/coolify/backups/coolify_pg_latest.sql.gz
              fi
              sudo rm -rf /tmp/prewarm_cache
            fi
          fi

          # Pre-pull standard Coolify container images to eliminate takeover delay
          echo "Pre-pulling core Coolify container images..."
          sudo docker network create --attachable coolify 2>/dev/null || true
          sudo docker pull ghcr.io/coollabsio/coolify:latest || true
          sudo docker pull ghcr.io/coollabsio/coolify-helper:latest || true
          sudo docker pull traefik:v3.1 || sudo docker pull traefik:v3.0 || true
          sudo docker pull postgres:15-alpine || sudo docker pull postgres:16-alpine || true
          sudo docker pull redis:7-alpine || sudo docker pull redis:alpine || true

          # Universal Pre-Pull: Dynamically scan restored compose files for user-deployed images
          if [ -d "/data/coolify/applications" ] || [ -d "/data/coolify/services" ]; then
            echo "Pre-pulling application images found in restored configurations..."
            for cmp in $(find /data/coolify/applications /data/coolify/services -name "docker-compose.yml" 2>/dev/null || true); do
              (cd "$(dirname "$cmp")" && sudo docker compose pull -q 2>/dev/null || true) &
            done
            wait
          fi

          # Strict Fail-Closed Check: Never bootstrap blank install if cycle > 0
          if [ ! -f "/data/coolify/source/docker-compose.yml" ]; then
            if [ "$CYCLE" != "0" ]; then
              echo "CRITICAL ERROR: Cycle is ${CYCLE} but no compose files exist! Aborting to trigger Circuit Breaker."
              exit 1
            fi

            echo "First-time installation authorized for Cycle 0. Bootstrapping Coolify..."
            curl -fsSL https://cdn.coollabs.io/coolify/install.sh | sudo bash -s -- -f || true

            if [ -f "/data/coolify/source/.env" ]; then
              sudo sed -i 's|^APP_URL=.*|APP_URL=https://coolify.justsawyou.cyou|g' /data/coolify/source/.env 2>/dev/null || true
              if ! grep -q 'APP_URL=' /data/coolify/source/.env; then
                echo "APP_URL=https://coolify.justsawyou.cyou" | sudo tee -a /data/coolify/source/.env >/dev/null
              fi
            fi

            echo "Awaiting Coolify container stack initialization..."
            for sec in {1..90}; do
              if sudo docker ps --format '{{.Names}}' | grep -q 'coolify'; then
                echo "Coolify containers successfully spun up!"
                break
              fi
              sleep 2
            done
          fi

          echo "Pre-warming completed successfully."

      # =====================================================================
      # CIRCUIT BREAKER: Dispatches fallback if Phase 1 failed
      # =====================================================================
      - name: Circuit Breaker on Pre-Warming Failure
        if: failure() && steps.prewarm.outcome == 'failure'
        env:
          NEXT_PAT: ${{ secrets.NEXT_PAT }}
          FALLBACK_PAT: ${{ secrets.FALLBACK_PAT }}
          CYCLE_COUNT: ${{ github.event.inputs.cycle_count }}
          RETRY_DEPTH: ${{ github.event.inputs.retry_depth }}
          TEST_HANDOVER: ${{ github.event.inputs.test_handover }}
        run: |
          echo "=== CIRCUIT BREAKER TRIGGERED ==="
          echo "Pre-warming failed. Predecessor is untouched and still serving."

          DEPTH=${RETRY_DEPTH:-0}
          if [ "$DEPTH" -ge 2 ]; then
            echo "Max retry depth ($DEPTH) reached. Halting automatic failover to prevent ring loop."
            exit 1
          fi

          NEW_DEPTH=$((DEPTH + 1))
          echo "Executing fallback dispatch to next ring node with retry_depth=${NEW_DEPTH}..."

          REPOS=(
            "ember-ops1/coolify-relay"
            "silver-weaver/coolify-relay"
            "moss-and-wire/coolify-relay"
            "pixel-crafter1/coolify-relay"
          )
          CURRENT_INDEX=-1
          for i in "${!REPOS[@]}"; do
            if [ "${REPOS[$i]}" = "${GITHUB_REPOSITORY}" ]; then
              CURRENT_INDEX=$i
              break
            fi
          done
          [ $CURRENT_INDEX -eq -1 ] && CURRENT_INDEX=0

          NEXT_INDEX=$(( (CURRENT_INDEX + 1) % ${#REPOS[@]} ))
          FALLBACK_INDEX=$(( (CURRENT_INDEX + 2) % ${#REPOS[@]} ))
          PRIMARY_TARGET="${REPOS[$NEXT_INDEX]}"
          FALLBACK_TARGET="${REPOS[$FALLBACK_INDEX]}"

          echo "Attempting primary dispatch to: ${PRIMARY_TARGET}..."
          sleep 15
          if ! GH_TOKEN="$NEXT_PAT" gh workflow run coolify-relay.yml \
            --repo "$PRIMARY_TARGET" \
            --ref main \
            -f cycle_count="${CYCLE_COUNT:-0}" \
            -f retry_depth="$NEW_DEPTH" \
            -f test_handover="${TEST_HANDOVER:-0}"; then
            echo "Primary dispatch failed. Activating secondary fallback: ${FALLBACK_TARGET}..."
            sleep 10
            GH_TOKEN="$FALLBACK_PAT" gh workflow run coolify-relay.yml \
              --repo "$FALLBACK_TARGET" \
              --ref main \
              -f cycle_count="${CYCLE_COUNT:-0}" \
              -f retry_depth="$NEW_DEPTH" \
              -f test_handover="${TEST_HANDOVER:-0}" || echo "CRITICAL: All fallback dispatches failed!"
          fi

          exit 1

      # =====================================================================
      # PHASE 2 & 3: HANDSHAKE, DELTA INGESTION & INGRESS TAKEOVER
      # =====================================================================
      - name: Handshake, State Hydration & Cloudflare Ingress Takeover
        env:
          PREV_PAT: ${{ secrets.PREV_PAT }}
          STORAGE_TARGET: ${{ secrets.STORAGE_TARGET }}
          CLOUDFLARE_TUNNEL_TOKEN: ${{ secrets.CLOUDFLARE_TUNNEL_TOKEN }}
          CYCLE_COUNT: ${{ github.event.inputs.cycle_count }}
        run: |
          set -euo pipefail
          echo "=== PHASE 2 & 3: Handshake, State Hydration & Ingress Claim ==="
          TARGET="${STORAGE_TARGET:-gdrive:coolify-relay-state/coolify-state}"
          CYCLE="${CYCLE_COUNT:-0}"

          # If rotation cycle > 0, await predecessor sync_complete.flag
          if [ "$CYCLE" != "0" ]; then
            echo "Awaiting predecessor delta synchronization flag on Google Drive..."
            FLAG_FOUND=false
            for sec in {1..30}; do
              if rclone cat --drive-use-trash=false "${TARGET}/control/sync_complete.flag" >/dev/null 2>&1; then
                echo "Predecessor sync_complete flag detected ($((sec * 6))s)."
                FLAG_FOUND=true
                break
              fi
              sleep 6
            done
            if [ "$FLAG_FOUND" != "true" ]; then
              echo "::error::Predecessor sync_complete flag timed out after 180s! Predecessor may still be uploading or failed."
              echo "Aborting state hydration to prevent downloading torn or corrupted state."
              exit 1
            fi
          fi

          # Hydrate state using hardened fail-closed restore script
          chmod +x scripts/coolify/restore_delta.sh
          ./scripts/coolify/restore_delta.sh "$TARGET" "$CYCLE"

          # Launch Cloudflare Tunnel connector
          if [ -n "$CLOUDFLARE_TUNNEL_TOKEN" ]; then
            echo "Starting Cloudflare Tunnel daemon with token..."
            sudo cloudflared tunnel run --token "$CLOUDFLARE_TUNNEL_TOKEN" > /tmp/cloudflared.log 2>&1 &
            sleep 6

            echo "Awaiting local Coolify dashboard (port 8000)..."
            COOLIFY_UP=false
            for chk in {1..45}; do
              if curl -fsS -o /dev/null --connect-timeout 2 http://localhost:8000; then
                echo "SUCCESS: Coolify dashboard is responding on port 8000 ($((chk*2))s)."
                COOLIFY_UP=true
                break
              fi
              sleep 2
            done
            [ "$COOLIFY_UP" != "true" ] && sudo docker ps -a

            echo "Verifying public Cloudflare tunnel endpoint..."
            CF_ONLINE=false
            for cf_chk in {1..30}; do
              CF_STATUS=$(curl -s -o /dev/null -w "%{http_code}" --connect-timeout 3 https://coolify.justsawyou.cyou || echo "000")
              echo "Public URL check (${cf_chk}/30): HTTP ${CF_STATUS}"
              if [ "$CF_STATUS" != "530" ] && [ "$CF_STATUS" != "000" ]; then
                echo "SUCCESS: Cloudflare tunnel is publicly active (HTTP ${CF_STATUS})!"
                CF_ONLINE=true
                break
              fi
              sleep 2
            done
            if [ "$CF_ONLINE" != "true" ]; then
              echo "::error::Cloudflare tunnel failed to report healthy after 60s (HTTP ${CF_STATUS})."
              cat /tmp/cloudflared.log || true
              exit 1
            fi
          fi

          # Clear handshake flag ONLY AFTER services and dashboard are verified healthy
          rclone delete --drive-use-trash=false "${TARGET}/control/sync_complete.flag" 2>/dev/null || true
          echo "Handshake flag cleared on Google Drive after health verification."

          # Telegram Alert: Runner is online and serving
          python3 scripts/coolify/notify_tg.py "🟢 <b>Coolify Node Online!</b>%0A• <b>Node:</b> ${GITHUB_REPOSITORY}%0A• <b>Cycle:</b> ${CYCLE}%0A• <b>Status:</b> All containers active & verified.%0A• <b>Shift Length:</b> ~5.5 hours (330 mins)." 2>/dev/null || true

          # Cancel predecessor runner run to release GitHub concurrency
          REPOS=(
            "ember-ops1/coolify-relay"
            "silver-weaver/coolify-relay"
            "moss-and-wire/coolify-relay"
            "pixel-crafter1/coolify-relay"
          )
          CURRENT_INDEX=-1
          for i in "${!REPOS[@]}"; do
            if [ "${REPOS[$i]}" = "${GITHUB_REPOSITORY}" ]; then
              CURRENT_INDEX=$i
              break
            fi
          done

          if [ $CURRENT_INDEX -ge 0 ] && [ -n "$PREV_PAT" ]; then
            PREV_INDEX=$(( (CURRENT_INDEX - 1 + ${#REPOS[@]}) % ${#REPOS[@]} ))
            PREV_REPO="${REPOS[$PREV_INDEX]}"
            echo "Cancelling predecessor active run on ${PREV_REPO}..."
            PREV_RUN_ID=$(GH_TOKEN="$PREV_PAT" gh run list --repo "$PREV_REPO" \
              --json databaseId,status -q '.[] | select(.status=="in_progress") | .databaseId' 2>/dev/null | head -n 1 || echo "")
            if [ -n "$PREV_RUN_ID" ] && [ "$PREV_RUN_ID" != "null" ]; then
              GH_TOKEN="$PREV_PAT" gh run cancel "$PREV_RUN_ID" --repo "$PREV_REPO" 2>/dev/null || true
            fi
          fi

      # =====================================================================
      # PHASE 4: 330m PRODUCTION SHIFT & WATCHDOG
      # =====================================================================
      - name: Production Shift & Successor Handover Dispatch
        env:
          NEXT_PAT: ${{ secrets.NEXT_PAT }}
          FALLBACK_PAT: ${{ secrets.FALLBACK_PAT }}
          STORAGE_TARGET: ${{ secrets.STORAGE_TARGET }}
          CYCLE_COUNT: ${{ github.event.inputs.cycle_count }}
          TEST_HANDOVER: ${{ github.event.inputs.test_handover }}
        run: |
          set -euo pipefail
          echo "=== PHASE 4: Production Shift Active ==="
          HANDOVER_MINUTE=330
          if [ -n "$TEST_HANDOVER" ] && [ "$TEST_HANDOVER" -gt 0 ] 2>/dev/null; then
            HANDOVER_MINUTE=$TEST_HANDOVER
          fi

          TARGET="${STORAGE_TARGET:-gdrive:coolify-relay-state/coolify-state}"
          CYCLE="${CYCLE_COUNT:-0}"
          ELAPSED=0

          while [ $ELAPSED -lt $HANDOVER_MINUTE ]; do
            sleep 60
            ELAPSED=$((ELAPSED + 1))
            REMAINING=$((HANDOVER_MINUTE - ELAPSED))

            # Telegram Countdown Alerts
            if [ $HANDOVER_MINUTE -ge 300 ]; then
              case $REMAINING in
                240) python3 scripts/coolify/notify_tg.py "⏳ <b>Shift Update:</b> 4 hours left until next runner handover (%0A• Current node: ${GITHUB_REPOSITORY})" 2>/dev/null || true ;;
                180) python3 scripts/coolify/notify_tg.py "⏳ <b>Shift Update:</b> 3 hours left until next runner handover (%0A• Current node: ${GITHUB_REPOSITORY})" 2>/dev/null || true ;;
                120) python3 scripts/coolify/notify_tg.py "⏳ <b>Shift Update:</b> 2 hours left until next runner handover (%0A• Current node: ${GITHUB_REPOSITORY})" 2>/dev/null || true ;;
                60)  python3 scripts/coolify/notify_tg.py "⏳ <b>Shift Update:</b> 1 hour left until next runner handover (%0A• Current node: ${GITHUB_REPOSITORY})" 2>/dev/null || true ;;
                30)  python3 scripts/coolify/notify_tg.py "⚠️ <b>Shift Update:</b> 30 minutes left until successor rotation (%0A• Current node: ${GITHUB_REPOSITORY})" 2>/dev/null || true ;;
                10)  python3 scripts/coolify/notify_tg.py "🚨 <b>Warning:</b> 10 minutes left until runner handover! Please avoid creating or modifying active deployments." 2>/dev/null || true ;;
                2)   python3 scripts/coolify/notify_tg.py "⚠️ <b>Notice:</b> 2 minutes left. Preparing state snapshot and runner transfer." 2>/dev/null || true ;;
              esac
            fi

            # Maintain SSH key synchronization
            sudo docker exec coolify cat /var/www/html/storage/app/ssh/keys/id_ed25519.pub 2>/dev/null | sudo tee -a /root/.ssh/authorized_keys /home/runner/.ssh/authorized_keys >/dev/null || true

            # Coolify health check every 2 minutes
            if [ $((ELAPSED % 2)) -eq 0 ]; then
              if ! curl -fsS --connect-timeout 2 http://localhost:8000 >/dev/null 2>&1; then
                echo "[$(date +'%T')] Warning: Local Coolify dashboard unreachable at minute ${ELAPSED}. Recovering container stack..."
                (cd /data/coolify/source && sudo docker compose --env-file .env -f docker-compose.yml -f docker-compose.prod.yml up -d 2>&1 || true)
              fi
            fi

            # Checkpoint backup every 30 minutes with 4-core pigz (staged file-backed copyto, no un-rewindable rcat)
            if [ $((ELAPSED % 30)) -eq 0 ]; then
              echo "[$(date +'%T')] Minute ${ELAPSED}: Executing periodic SQL checkpoint to Google Drive..."
              if sudo docker ps --format '{{.Names}}' | grep -q 'coolify-db'; then
                sudo mkdir -p /tmp/sql_checkpoints
                sudo chmod 777 /tmp/sql_checkpoints
                sudo docker exec coolify-db pg_dumpall -U coolify --clean 2>/dev/null | pigz -p 4 -6 > /tmp/sql_checkpoints/coolify_pg_latest.sql.gz
                if [ -s /tmp/sql_checkpoints/coolify_pg_latest.sql.gz ]; then
                  rclone copyto --drive-chunk-size=128M --drive-use-trash=false --retries=5 --low-level-retries=10 --tpslimit=8 \
                    /tmp/sql_checkpoints/coolify_pg_latest.sql.gz "${TARGET}/coolify_pg_latest.sql.gz" 2>/dev/null || true
                  echo "[$(date +'%T')] Periodic SQL checkpoint verified & uploaded ($(du -sh /tmp/sql_checkpoints/coolify_pg_latest.sql.gz | cut -f1))."
                else
                  echo "[$(date +'%T')] Warning: Checkpoint dump resulted in empty file. Skipping upload."
                fi
                sudo rm -rf /tmp/sql_checkpoints
              fi
            fi
          done

          echo "=== HANDOVER THRESHOLD REACHED (${HANDOVER_MINUTE}m) ==="
          python3 scripts/coolify/notify_tg.py "❄️ <b>FREEZING AND BACKING UP!</b>%0APlease do not make any changes or deployments right now. Syncing universal delta to Google Drive and dispatching successor..." 2>/dev/null || true

          # 1. Execute Atomic Delta Freeze & Upload to Google Drive
          echo "Executing Multi-Core Delta Freeze & Sync..."
          chmod +x scripts/coolify/sync_delta.sh
          ./scripts/coolify/sync_delta.sh "$TARGET"

          # 2. Create synchronization flag on Google Drive
          rclone touch --drive-use-trash=false "${TARGET}/control/sync_complete.flag" 2>/dev/null || true

          # 3. Dispatch successor runner across the ring BEFORE releasing ingress
          REPOS=(
            "ember-ops1/coolify-relay"
            "silver-weaver/coolify-relay"
            "moss-and-wire/coolify-relay"
            "pixel-crafter1/coolify-relay"
          )
          CURRENT_INDEX=-1
          for i in "${!REPOS[@]}"; do
            if [ "${REPOS[$i]}" = "${GITHUB_REPOSITORY}" ]; then
              CURRENT_INDEX=$i
              break
            fi
          done
          [ $CURRENT_INDEX -eq -1 ] && CURRENT_INDEX=0

          NEXT_INDEX=$(( (CURRENT_INDEX + 1) % ${#REPOS[@]} ))
          FALLBACK_INDEX=$(( (CURRENT_INDEX + 2) % ${#REPOS[@]} ))
          PRIMARY_TARGET="${REPOS[$NEXT_INDEX]}"
          FALLBACK_TARGET="${REPOS[$FALLBACK_INDEX]}"
          NEXT_CYCLE=$((CYCLE + 1))

          dispatch_runner() {
            local target="$1"
            local token="$2"
            for attempt in 1 2 3 4; do
              echo "Dispatch attempt $attempt to ${target}..."
              if GH_TOKEN="$token" gh workflow run coolify-relay.yml \
                --repo "$target" \
                --ref main \
                -f cycle_count="$NEXT_CYCLE" \
                -f retry_depth="0" \
                -f test_handover="${TEST_HANDOVER:-0}"; then
                echo "Successfully triggered workflow on ${target}."
                return 0
              fi
              sleep 15
            done
            return 1
          }

          DISPATCH_SUCCESS=false
          echo "Dispatching successor runner to ${PRIMARY_TARGET}..."
          if dispatch_runner "$PRIMARY_TARGET" "$NEXT_PAT"; then
            DISPATCH_SUCCESS=true
          else
            echo "Primary dispatch failed. Activating fallback: ${FALLBACK_TARGET}..."
            if dispatch_runner "$FALLBACK_TARGET" "$FALLBACK_PAT"; then
              DISPATCH_SUCCESS=true
            fi
          fi

          if [ "$DISPATCH_SUCCESS" != "true" ]; then
            echo "CRITICAL: All successor dispatches failed! Ingress will remain ACTIVE on this runner."
            rclone delete --drive-use-trash=false "${TARGET}/control/sync_complete.flag" 2>/dev/null || true
            exit 1
          fi

          # 4. Successor dispatched successfully: release Cloudflare Tunnel connector
          echo "Successor successfully dispatched. Releasing Cloudflare Tunnel connector to prevent traffic collision..."
          sudo pkill -SIGTERM cloudflared 2>/dev/null || true

          # 5. Await successor handshake claim
          echo "Awaiting successor to claim delta flag on Google Drive..."
          for check in {1..60}; do
            if ! rclone cat --drive-use-trash=false "${TARGET}/control/sync_complete.flag" >/dev/null 2>&1; then
              echo "Successor has claimed state. Exiting cleanly..."
              break
            fi
            sleep 5
          done

          exit 0

      # =====================================================================
      # PHASE 5: TEARDOWN
      # =====================================================================
      - name: Final Teardown Safety Check
        if: always()
        run: |
          echo "=== PHASE 5: Teardown ==="
          sudo pkill -SIGTERM cloudflared 2>/dev/null || true
          (cd /data/coolify/source 2>/dev/null && sudo docker compose down -t 2) || true
